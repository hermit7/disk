<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ise.mapper.UserMapper">
	<resultMap id="BaseResultUserMap" type="com.ise.pojo.User">
		<id column="user_id" jdbcType="VARCHAR" property="userId" />
		<result column="user_name" jdbcType="VARCHAR"
			property="username" />
		<result column="password" jdbcType="VARCHAR"
			property="password" />
		<result column="remain_space" jdbcType="VARCHAR"
			property="space" />
		<result column="user_type" jdbcType="VARCHAR" property="type" />
		<result column="nick_name" jdbcType="VARCHAR"
			property="nickname" />
	</resultMap>

	<select id="login" resultMap="BaseResultUserMap">
		SELECT `user_id`,`user_name`,`password`,`remain_space`,`user_type` 
		FROM `user`
		WHERE `user_name` = #{username}
		AND `password` = #{password}
	</select>
	
	<select id="findUser" parameterType="String" resultMap="BaseResultUserMap">
		SELECT `user_id`,`user_name` FROM `user`
		WHERE `user_name` = #{username}
	</select>
	
	<select id="findAllUsers" resultType="User">
		SELECT `user_id`,`user_name`,`password`,`remain_space`,`user_type` 
		FROM `user`
	</select>
	
	<insert id="addUser" parameterType="User">
		INSERT INTO `user`(`user_name`,`password`,`remain_space`,`user_type`) 
		values(#{username},#{password},#{space},#{type}) 
	</insert>
	
	<delete id="deleteUser" parameterType="Integer">
		DELETE FROM `user` WHERE `user_id` = ${userId}
	</delete>
	
</mapper>